SOVEREIGN PATCH AGENT — Demo Script
Recording: Screen + Voiceover | Duration: ~1 minute
================================================

[0:00 — INTRODUCTION]

(start screen recording — show playground at http://127.0.0.1:18081)

"This is Sovereign Patch Agent — an autonomous security system
that finds vulnerabilities in your code, generates fixes,
and lets a human approve before anything goes to production.

Built with Google ADK 2.2, Gemini 2.5 Flash, and MCP."


[0:15 — ARCHITECTURE]

(show assets/architecture_diagram.svg)

"It uses a Workflow graph with four nodes:
SecurityGate — the first line of defence.
VulnerabilityScanner — finds security flaws.
PatchGenerator — creates fixes in a sandbox.
CodeReviewer — final check and human approval."

(pause 2 seconds)


[0:30 — LIVE DEMO]

(switch to playground at http://127.0.0.1:18081)

"Let me scan this Python code for vulnerabilities."

(paste this code in the playground):

import sqlite3
def get_user(username):
    conn = sqlite3.connect("users.db")
    cursor = conn.cursor()
    cursor.execute(f"SELECT * FROM users WHERE name = '{username}'")
    return cursor.fetchone()

"SecurityGate passes it — clean input, no injection or PII."

(wait for response)

"VulnerabilityScanner finds SQL injection at line 6 —
f-string in cursor.execute(). Matches CWE-89."

(wait for response)

"PatchGenerator creates a parameterised query fix."

(wait for response)

"CodeReviewer approves and asks for human sign-off."

(pause 2 seconds)


[0:55 — SECURITY AND MCP]

"The SecurityGate scrubs six PII types, detects nine injection patterns,
and logs every decision to a JSON audit trail.

The MCP Server has five tools — scan_codebase, run_sandbox_test,
stage_git_pull_request, get_vulnerability_db, and get_audit_log."

(pause 2 seconds)


[1:05 — CLOSE]

"Who benefits? Developers, security teams, and organisations
that need compliance-ready audit trails.

Sovereign Patch Agent — built with Google ADK 2.2.
Thank you."

(stop recording)


================================================
RECORDING CHECKLIST
================================================
[ ] Start recording — playground visible
[ ] Show architecture diagram (5 seconds)
[ ] Switch back to playground
[ ] Paste SQL injection code
[ ] Wait for agent response
[ ] Narrate each agent's action
[ ] Show final approval prompt
[ ] Stop recording
================================================
